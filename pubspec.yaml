name: flutter_localization
description: Flutter Localization
version: 2.1.0
homepage: www.credeo.rs

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  devicelocale: ^0.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: